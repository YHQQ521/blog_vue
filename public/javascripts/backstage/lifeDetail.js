$(function(){
  //获取数据
      $.ajax({
        type:'GET',
        dataType:'json',
        data:{data:adId},
        url:'/admin/htgetLife2',
        success:function(response,status,xhr){
          console.log(response);
          $('#imgSamllSrc').append('<img src="../'+response[0].cover_photo+'" alt="">');
          $('#adTitle').text(response[0].title);
          $('#summary').text(response[0].brief);
          $('#selectedName').text(response[0].class_name);
          $('#adDescription').html(response[0].content);
          $('#zanTotal').text(response[0].zan);
        }
      })
    $('.back').click(function(){
        window.close();
    })
})